# 依赖
node_modules/

# 需求规格（内部文档，不公开）
AutoTest需求规格.txt

# 本地开发工具（第三方 skill 下载、临时脚本，不入库）
.tools/

# 日志
*.log
*.log.err

# 一次性测试产物
schtasks-test.txt

# 本地 npm pack 产物（发布由 CI 从 tag 构建，tar 包不入库）
*.tgz

# SQLite 种子库（首次启动自动建表 + 种子，不入库）
autotest/dsh-autotest/data/
# 数据目录迁出插件包时的旧目录留底（不入库）
autotest/dsh-autotest/data.migrated-*/
# 数据目录自检沙箱（假安装树）
autotest/dsh-autotest/.verify-data-dir-*/

# 运行时工作区（仓库克隆 / 自动化脚本落盘）
workspace/

# 构建缓存
*.tsbuildinfo
.vite-temp/

# OS
.DS_Store
Thumbs.db
