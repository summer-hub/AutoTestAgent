# 2026-09-01

- 诊断 explore_cases（真机遍历生成用例 Agent）与 optimizeCaseById（用例优化 Agent）数量/质量不达标根因：
  - 数量：一次性 LLM 调用生成全部用例但 maxTokens=4000 必截断；解析失败重试降级为 sysCompact「4 条精简用例」；reviewAndRefine 用截断的 revised 列表整表替换完整草稿（executor.ts:284 `if (revised.length > 0) cur = revised`）；insertDraftCases 里 maxCasesPerTask=20 硬截断。
  - 质量：无确定性校验（控件引用∈controls 靠 prompt 自觉）；pageSignature 仅按文本集合去重，Tab/动态页误判重复漏页；optimizeCaseById 不知用例对应页面，注入前 10 页全部控件清单，且优化后 scriptStatus=未绑定却不重生成脚本。
  - 契约缺失：hypiumGen.stepToPython 正则只认固定中文句式，生成端没有步骤动词白名单约束 → 大量"未映射步骤"注释行。
  - 追踪：traceTask 对 tasks.trace JSON 列读改写无并发保护；无 traceId/spanId；AI 返回只存前 600 字符；lastLlmCall 全局变量并发竞态（llmHarness.ts:21）；llm-raw.json 硬编码绝对路径（llmHarness.ts:144）。
- 结论：应借鉴 DSH/harness 工程思维——执行器为真（生成→脚本→真机 dry-run→失败回灌）、确定性 guardrails 先于 LLM 自审、按页分片生成替代 one-shot。
- 建议改造清单已给用户（见对话），核心：按页分片 map-reduce 生成、逐条评审增量 merge、case 记录 pagePath 溯源、agent_events 追加式事件表、LLM 调用层埋点、步骤句式契约白名单。
- 落地 P0（计划见 docs/优化计划.md），改动文件：`autotest/dsh-autotest/src/services/executor.ts`、`hypiumGen.ts`、`settings.ts`，新增 `scripts/verify-step-contract.mjs`：
  - exploreCases 改为按页分片生成（新配置 `agent.genPagesPerShard` 默认 2，范围 1-6），每页按丰富度配额（controls>8 或 scrolls>0 → 2 条），单片失败只重试该片，不再全局降级为「4 条精简」。
  - 新增 `validateDraftsAgainstPages` 确定性 guardrail + `repairDrafts`（违规用例回灌该页真实控件清单重写一次，复检不合格才丢弃），放在 LLM 自审之前。
  - reviewAndRefine 改增量合并：评审只输出带 index 的问题条目，按 index 替换，解析失败/缺 index 保留原稿。
  - DraftCase 增加 pagePath 溯源字段，写入 case_versions.snapshot；optimizeCaseById 据此只注入所属页面控件清单，优化后自动重绑脚本（script_status=已绑定）。
  - 新增 STEP_CONTRACT 句式契约常量并注入所有生成/优化 prompt；hypiumGen 支持 `输入「内容」到「控件」` 二段式；「打开应用」由注释行改为真实 start_app。
  - 自检中修出两个真实缺陷：二段式输入的内容段被误当控件校验（stepControlRefs）、单字符控件让子串匹配恒真放行（现要求 ≥2 字才参与子串匹配）。
- 验证方式：`npm run build` 通过；`npm run verify:contract` 全通过。该脚本不连 DB、不调 LLM，适合做 CI 契约回归门禁。
- dry-run 闭环（P1 核心）已在模拟器（127.0.0.1:5555）落地并实测：
  - 新增 `services/dryRun.ts`：`dryRunCase` 逐步执行 + **每个失败点抓取界面真实可见控件**，产出 `failureBrief` 回灌 LLM；连续失败 N 步后停止（界面已偏离，后续证据不可信）。`mergeFailureBriefs` 汇总多条用例。
  - `executor.ts` 新增 `dryRunAndRepair`（入库后对前 N 条真跑）+ `repairFromDryRun`（重写、升版本、重绑脚本）；无设备自动跳过不阻断主流程，整体包 try/catch。
  - `hdc.ts` 导出 `runStepWithTimeout`（整批 `executeCaseSteps` 是黑盒，拿不到失败点的界面证据）。
  - 新配置：`agent.dryRunEnabled`(true) / `dryRunMaxCases`(5) / `dryRunPerStepTimeoutMs`(15000) / `dryRunFailStreakStop`(2)。
  - **模拟器实测抓出致命回归**：`hdc.pickKeyword` 不剥离「」引号，而 P0 新下发的 STEP_CONTRACT 强制 `点击「X」`/`验证「X」`，关键词变成「「X」」→ 所有点击/验证步骤 100% 假失败，闭环会退化成噪声。已修；连带修二段式 `输入「A」到「B」` 把"到 B"当输入内容（现先定位控件聚焦再输入）。
  - 自检脚本扩展第 3 组「执行端 dry-run 真机判定」（无设备则 SKIP），共 19 项全通过。
  - 新增 `scripts/dryrun-smoke.mjs`：不带参数只 dump 界面控件，带步骤参数则真跑并打印失败简报——手工验证 dry-run 用。
- 环境事实：本机 Python 3.13.14 可用，但 **xdevice / hypium 未安装** → Hypium 脚本链路（hypiumRunner）当前跑不了；dry-run 必须走 hdc 引擎（不依赖 Python）。
- 待办 P1 剩余：pageSignature 结构指纹、agent_events 追加式事件表 + LLM 埋点、lastLlmCall 改为随调用返回、llm-raw.json 路径改 workspace 相对。
